/** @type {import('next').NextConfig} */
const nextConfig = {
	reactStrictMode: true,
}

module.exports = nextConfig

/* Google Fontsの有効化 */
module.exports = {
	experimental: {
		optimizeFonts: true,
	},
};
